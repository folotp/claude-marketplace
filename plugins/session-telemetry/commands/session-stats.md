---
description: Append a structured NDJSON snapshot of the current session to the Organon vault telemetry log
argument-hint: [--tag <tag>]...
---

Load the `session-telemetry:session-telemetry` skill via the Skill tool and follow it end-to-end. Forward all `--tag` arguments to the skill.
